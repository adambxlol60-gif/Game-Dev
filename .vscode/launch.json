{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Game",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/game",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build and run"
        }
    ]
}
